[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/submit-quote"
  to = "/.netlify/functions/submit-quote"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
